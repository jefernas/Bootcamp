server:
  port: 8086
spring:
  config:
   import: configserver:http://localhost:8888
  application:
    name: product-service2
 
  cloud:
   config:
    enabled: true
    uri: http://localhost:8888
   
    

   

  

